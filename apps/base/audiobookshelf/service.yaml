apiVersion: v1
kind: Service
metadata:
  name: service-abs 
  namespace: audiobookshelf
spec:
  ports:
  - port: 3005 
    protocol: TCP
    targetPort: 3005
  selector:
    app: audiobookshelf
  type: ClusterIP
